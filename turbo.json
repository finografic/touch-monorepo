{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": ["NODE_ENV"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "@workspace/types#build": {
      "outputs": ["dist/**", "*.tsbuildinfo"]
    },
    "@workspace/globals#build": {
      "dependsOn": ["@workspace/types#build"],
      "outputs": ["dist/**", "*.tsbuildinfo"]
    },
    "@workspace/config#build": {
      "dependsOn": ["@workspace/globals#build"],
      "outputs": ["dist/**", "*.tsbuildinfo"]
    },
    "@workspace/i18n#build": {
      "dependsOn": ["@workspace/types#build", "@workspace/config#build"],
      "outputs": ["dist/**", "*.tsbuildinfo"]
    },
    "@workspace/core#build": {
      "dependsOn": ["@workspace/types#build", "@workspace/config#build", "@workspace/i18n#build"],
      "outputs": ["dist/**", "*.tsbuildinfo"]
    },
    "dev": {
      "cache": false,
      "persistent": true,
      "dependsOn": ["@workspace/core#build"],
      "env": ["NODE_ENV", "PORT"]
    },
    "lint": {
      "outputs": []
    },
    "clean": {
      "cache": false
    }
  }
}
