import { Navigation } from 'components/Navigation/Navigation';
import { styles } from './Footer.styles';

export const Footer = () => {
  return (
    <footer css={styles}>
      <Navigation />
    </footer>
  );
};
