import { css } from '@emotion/react';

export const styles = css`
  z-index: 9999999;
  position: relative;
  bottom: 3rem;
  left: 0;
  right: 0;
  display: flex;
  justify-content: center;
  padding: 1rem;

  .controls {
    display: flex;
    gap: 2rem;
  }
`;
