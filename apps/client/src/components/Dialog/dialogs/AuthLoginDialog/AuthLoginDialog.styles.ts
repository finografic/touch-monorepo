import { css } from '@emotion/react';

export const styles = {
  container: css`
    padding: 1rem;
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
    color: var(--gray-12);
  `,

  loginForm: css`
    width: 100%;
    max-width: 400px;
    margin: 0 auto;
  `,
};
