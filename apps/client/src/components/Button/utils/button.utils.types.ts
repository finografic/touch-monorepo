import type { ButtonColor } from '../Button.types';

export type ButtonColorVariants =
  | `${ButtonColor}Light`
  | `${ButtonColor}XLight`
  | ButtonColor
  | `${ButtonColor}Dark`;

// export type ButtonColorVariants =
//   | `${ButtonColor}Light`
//   | `${ButtonColor}XLight`
//   | ButtonColor
//   | `${ButtonColor}Dark`
//   | `${ButtonColor}XDark`;
