/**
 * Asset paths for static resources
 * These are paths to static assets served by the application
 */

export const ASSET_PATHS = {
  IMAGES: '/images',
  ICONS: '/icons',
  SOUNDS: '/sounds',
  FONTS: '/fonts',
} as const;
