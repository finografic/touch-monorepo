import { css } from '@emotion/react';

export const styles = css`
  .admin-slot-config {
    max-width: 1200px;
    margin: 0 auto;
    padding: 1rem;
  }

  .loading,
  .error {
    text-align: center;
    padding: 2rem;
  }

  .error {
    color: var(--red-9);
  }
`;
