// Re-export env.shared from local copy for bundling compatibility
// export { envShared, paths } from '../env.shared.js';
export { envShared } from '@workspace/config/env.shared';
export { paths } from '@workspace/config/paths';
